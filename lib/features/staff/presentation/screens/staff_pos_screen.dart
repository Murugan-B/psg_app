import 'package:flutter/material.dart';

class StaffPosScreen extends StatelessWidget {
  const StaffPosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Staff POS')),
      body: const Center(child: Text('Staff POS')),
    );
  }
}
