# Icon Inventory

*Extracted directly from React Native Source via AST analysis. Zero AI assumptions.*

## Library Used
- `react-native-vector-icons/MaterialCommunityIcons`

## SVG Icons Used
- Built-in SVGs in `src/assets/` (`bell.svg`, `pos.svg`, `report.svg`, `cart.svg`, `person.svg`, `edit.svg`, `delete.svg`, `qr.svg`, etc.)

## Usage
- Reused across Tab Bars (via NavBar component) and Card elements.
